# Kubernetesにおける高度なスケジューリング

Kubernetesのスケジューラは、新しく作成されたPodをどのノードで実行するかを決定する重要なコンポーネントです。デフォルトでは、スケジューラはリソース要求（CPU, メモリ）を満たすノードにPodを自動的に配置します。

しかし、実際の運用では「特定のハードウェアを持つノードにPodを配置したい」「関連性の高いPod同士を近くに配置して通信を高速化したい」「Podを複数のゾーンに分散させて可用性を高めたい」といった、より高度な制御が必要になることがあります。

このドキュメントでは、そうした要件を実現するためのKubernetesの高度なスケジューリング機能について解説します。

## 1. 特定のノードにPodを配置する (Node Affinity)

特定のラベルを持つノードにPodを配置するための機能です。

### `nodeSelector`

最もシンプルで直接的な方法です。Podの`.spec.nodeSelector`フィールドに、ターゲットノードが持つべきラベルを`key: value`形式で指定します。

**例：SSDディスクを持つノードにPodを配置する**

`nodeSelector`を使用するには、以下の2つのステップを順番に実行します。

**ステップ1：ノードにラベルを付与する**
まず、Podを配置したいノードに識別用のラベルを付けます。この操作はPodを作成する前に行う必要があります。
```bash
kubectl label nodes <your-node-name> disktype=ssd
```

**ステップ2：Podマニフェストで配置先ノードを指定する**
次に、Podの定義ファイルで、ステップ1で付与したラベルを`nodeSelector`に指定します。
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: nginx-ssd
spec:
  containers:
  - name: nginx
    image: nginx
  nodeSelector:
    disktype: ssd
```
このPodを作成すると、スケジューラは`disktype=ssd`というラベルを持つノードを探して、そこにPodを配置します。

### `nodeAffinity`

`nodeSelector`よりも表現力豊かで柔軟なルールを設定できます。ルールには「必須条件」と「推奨条件」の2種類があります。

- **`requiredDuringSchedulingIgnoredDuringExecution`**: **必須条件**。このルールを満たすノードがない場合、Podはスケジュールされません（Pending状態になります）。
- **`preferredDuringSchedulingIgnoredDuringExecution`**: **推奨条件**。スケジューラはこのルールを満たすノードを優先的に選択しようと試みますが、満たすノードがなくても、他のノードにスケジュールされます。

**例：ゾーンAまたはBにあり、かつGPU搭載ノードを優先する**

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: nginx-affinity
spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: topology.kubernetes.io/zone
            operator: In
            values:
            - antarctica-east1
            - antarctica-west1
      preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 1
        preference:
          matchExpressions:
          - key: accelerator
            operator: In
            values:
            - nvidia-tesla-k80
  containers:
  - name: nginx
    image: nginx
```

**【この設定の解説】**

このマニフェストは、2つのルールを組み合わせています。

1.  **必須条件 (`requiredDuringSchedulingIgnoredDuringExecution`)**
    *   `key: topology.kubernetes.io/zone` は、ノードが属するゾーンを示す標準的なラベルです。
    *   `operator: In` は、`values`リストのいずれかの値に一致することを意味します。
    *   これにより、「`topology.kubernetes.io/zone` ラベルの値が `antarctica-east1` **または** `antarctica-west1` であるノードにしかスケジュールしない」という**絶対的なルール**が設定されます。

2.  **推奨条件 (`preferredDuringSchedulingIgnoredDuringExecution`)**
    *   `key: accelerator` は、ここではGPUの種類を示すカスタムラベルを想定しています。
    *   このルールにより、上記の必須条件を満たすノードの中から、「`accelerator` ラベルの値が `nvidia-tesla-k80` であるノードを**優先的に選択する**」という**努力目標**が設定されます。
    *   `weight: 1` はこのルールの優先度を示します（複数の推奨条件がある場合に、どのルールをより重視するかを数値で指定します）。

結果として、このPodはまずゾーンAまたはBにあるノードに絞り込まれ、その中でGPU搭載ノードがあれば最優先でそちらに配置され、なければゾーンA/Bの他のノードに配置される、という挙動になります。

## 2. Pod同士を近づける/遠ざける (Pod Affinity / Anti-affinity)

すでに実行中のPodのラベルを基準に、新しいPodをどこに配置するかを決定する機能です。

### `podAffinity`

指定したラベルを持つPodが実行されている「トポロジー領域（例: ノード、ゾーン）」に、新しいPodをスケジュールします。Webサーバーとキャッシュサーバーのように、頻繁に通信するPod同士を同じノードに配置してネットワーク遅延を減らす、といった用途で使われます。

**例：`service=backend`のPodと同じノードに配置する**

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: frontend
spec:
  affinity:
    podAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
      - labelSelector:
          matchExpressions:
          - key: service
            operator: In
            values:
            - backend
        topologyKey: kubernetes.io/hostname
  containers:
  - name: nginx
    image: nginx
```

### `podAntiAffinity`

`podAffinity`とは逆に、指定したラベルを持つPodが実行されているトポロジー領域を**避けて**スケジュールします。アプリケーションのレプリカを異なるノードやゾーンに分散させ、単一障害点（SPOF）をなくし、可用性を高める目的で使われます。

**例：`app=myapp`のPodとは異なるノードに配置する**

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - myapp
            topologyKey: "kubernetes.io/hostname"
      containers:
      - name: nginx
        image: nginx
```

## 3. Podを均等に分散させる (Topology Spread Constraints)

`podAntiAffinity`が「同じか、違うか」の二択で配置を制御するのに対し、この機能はPod群を複数のトポロジー領域（ノード、ゾーン、リージョンなど）に**できるだけ均等に**分散させることができます。

**例：`app=myapp`のPodをゾーン内で均等に分散させる**

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: mypod
  labels:
    app: myapp
spec:
  topologySpreadConstraints:
  - maxSkew: 1
    topologyKey: topology.kubernetes.io/zone
    whenUnsatisfiable: DoNotSchedule
    labelSelector:
      matchLabels:
        app: myapp
  containers:
  - name: pause
    image: k8s.gcr.io/pause:3.1
```
- `maxSkew`: 各トポロジー領域内のPod数の差の最大許容値。`1`は完全に均等に近づけることを意味します。
- `whenUnsatisfiable`: 制約を満たせない場合の動作（`DoNotSchedule`または`ScheduleAnyway`）。

## 4. 特定のノードへの配置を制限/許可する (Taints / Tolerations)

ノード側でPodの受け入れを制御する仕組みです。

### `Taints` (汚染)

ノードに`Taint`を設定すると、その`Taint`を許容（`Tolerate`）しないPodは、そのノードにスケジュールされなくなります。GPU搭載ノードや、マスターノードなど、特定のワークロード専用にしたいノードに設定します。

**例：ノードに`Taint`を設定する**

Taintは以下のコマンド形式で設定します。
```bash
# 書式
kubectl taint nodes <対象ノード名> <lable名>=<labelの値>:<Taintの効果>

# 具体例: GPUノードに設定する場合
kubectl taint nodes <your-node-name> dedicated=gpu:NoSchedule
```
- **Effect**:
    - `NoSchedule`: 新しいPodをスケジュールしない。
    - `PreferNoSchedule`: できるだけスケジュールしない（推奨）。
    - `NoExecute`: 新しいPodをスケジュールせず、すでに実行中のPodも（Tolerationがなければ）退去させる。

### `Tolerations` (許容)

Podの`.spec.tolerations`フィールドに`Toleration`を設定すると、対応する`Taint`を持つノードにもスケジュールされるようになります。

**例：Podに`Toleration`を設定する**

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: gpu-pod
spec:
  containers:
  - name: cuda-container
    image: nvidia/cuda:10.0-base
  tolerations:
  - key: "dedicated"
    operator: "Equal"
    value: "gpu"
    effect: "NoSchedule"
```

## 5. Podの重要度を定義する (Priority / Preemption)

クラスタのリソースが不足している状況で、どのPodを優先的にスケジュールするかを制御する仕組みです。

### `PriorityClass`

まず、`PriorityClass`オブジェクトを作成して、優先度の名前と数値を定義します。数値が大きいほど優先度が高くなります。

**例：`high-priority`クラスを作成する**

```yaml
apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: high-priority
value: 1000000
globalDefault: false
description: "This priority class should be used for critical service pods only."
```

### Podに優先度を割り当てる

Podの`.spec.priorityClassName`フィールドに、作成した`PriorityClass`の名前を指定します。

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: nginx-high-priority
spec:
  containers:
  - name: nginx
    image: nginx
  priorityClassName: high-priority
```

### `Preemption` (退去)

スケジューラが、優先度の高いPodを配置するためのリソースを確保できない場合、配置先のノードで実行中の**優先度の低いPodを強制的に終了（退去）**させて、リソースを確保しようとします。これが`Preemption`です。

## 6. 機能の使い分けまとめ

| 目的 | 使用する機能 | ユースケース |
| :--- | :--- | :--- |
| **特定のノードに配置したい** | `nodeSelector`, `nodeAffinity` | SSDやGPUなど特定のハードウェアを持つノードにPodを配置する。 |
| **Pod同士を近づけたい** | `podAffinity` | Webサーバーとキャッシュなど、通信が多いPodを同じノードに配置する。 |
| **Pod同士を遠ざけたい** | `podAntiAffinity` | アプリケーションのレプリカを別々のノードに分散させ、可用性を高める。 |
| **Podを均等に分散させたい** | `topologySpreadConstraints` | 複数のゾーンやラックにPodを均等にばらまき、障害の影響を局所化する。 |
| **特定のノードを使わせたくない** | `Taints` | マスターノードや特定用途のノードに、関係ないPodが配置されるのを防ぐ。 |
| **Taint付きノードを使いたい** | `Tolerations` | `Taint`が付いた専用ノードに、特定のPodの配置を許可する。 |
| **Podの重要度を決めたい** | `PriorityClass`, `Preemption` | システムコンポーネントや重要なアプリを優先的にスケジュールし、リソース不足時も稼働させる。 |
