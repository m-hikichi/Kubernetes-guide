# ConfigMap: 設定情報の管理

## 1. ConfigMapとは？

ConfigMapは、設定データ（コンフィグレーション）をキーと値のペアとして保存するためのKubernetesリソースです。アプリケーションコードと設定情報を分離することで、ポータビリティの高いアプリケーションを構築できます。

例えば、以下のような情報を管理するのに役立ちます。

*   データベースの接続先URL
*   機能の有効/無効を切り替えるフラグ
*   設定ファイル（例: `nginx.conf`）の内容

## 2. ConfigMapの作成方法

実運用では、マニフェストファイル（YAML）を使ってConfigMapを作成するのが一般的です。この方法は、Gitなどでバージョン管理ができ、宣言的にインフラを管理する「Infrastructure as Code」の実践に繋がるため、再現性が求められる本番環境や開発環境で**強く推奨**されます。

`configmap.yaml`
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: my-app-config
data:
  # キーと値のペアを直接記述
  DB_HOST: mysql.example.com
  DB_PORT: "3306"
  # ファイルの内容のように複数行のデータを記述することも可能
  nginx.conf: |
    server {
      listen 80;
      server_name example.com;
      root /var/www/html;
    }
```

> **Tips: 複数のリソースを1つのYAMLファイルにまとめる**
>
> Kubernetesでは、`---`区切りを使うことで、1つのYAMLファイル内にPodやConfigMapなど、複数のリソース定義を記述できます。関連するリソースをまとめて管理できるため便利です。
>
> ```yaml
> # --- Podの定義 ---
> apiVersion: v1
> kind: Pod
> metadata:
>   name: my-pod-with-env
> spec:
>   containers:
>   - name: my-container
>     image: busybox
>     command: [ "/bin/sh", "-c", "env" ]
>     env:
>       - name: DATABASE_HOST # コンテナ内で使われる環境変数名
>         valueFrom:
>           configMapKeyRef:
>             name: my-app-config # このファイル内で定義されているConfigMapを参照
>             key: DB_HOST        # ConfigMap内のどのキーの値を取得するかを指定
> ---
> # --- ConfigMapの定義 ---
> apiVersion: v1
> kind: ConfigMap
> metadata:
>   name: my-app-config
> data:
>   DB_HOST: mysql.example.com
>   DB_PORT: "3306"
> ```
>
> このように記述することで、`kubectl apply -f <ファイル名>` を一度実行するだけで、PodとConfigMapの両方を同時に作成・更新できます。

作成したYAMLファイルを`apply`コマンドで適用します。

```bash
kubectl apply -f configmap.yaml
```

## 3. PodからConfigMapを利用する方法

作成したConfigMapは、主に環境変数またはボリュームとしてPod内のコンテナに渡します。

### 3.1. 環境変数として読み込む

#### 特定のキーの値を環境変数に設定する (`valueFrom`)

ConfigMap内の特定のキーを選んで、コンテナの環境変数として設定します。

`pod-env.yaml`
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-pod-with-env
spec:
  containers:
  - name: my-container
    image: busybox
    command: [ "/bin/sh", "-c", "env" ] # コンテナ起動時に環境変数を一覧表示
    env:
      - name: DATABASE_HOST # コンテナ内で使われる環境変数名
        valueFrom:
          configMapKeyRef:
            name: my-app-config # どのConfigMapを参照するかを指定
            key: DB_HOST       # ConfigMap内のどのキーの値を取得するかを指定
```

#### すべてのキーと値を環境変数として展開する (`envFrom`)

ConfigMap内のすべてのキーと値のペアを、そのまま環境変数としてコンテナに展開します。

`pod-envfrom.yaml`
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-pod-with-envfrom
spec:
  containers:
  - name: my-container
    image: busybox
    command: [ "/bin/sh", "-c", "env" ]
    envFrom:
      - configMapRef:
          name: my-app-config # 利用するConfigMap名
```

### 3.2. ボリュームとしてマウントする

ConfigMapのデータをファイルとしてコンテナ内の特定のパスにマウントします。設定ファイルなどをコンテナに渡す場合に便利です。

`pod-volume.yaml`
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-pod-with-volume
spec:
  containers:
  - name: my-container
    image: nginx
    volumeMounts:
    - name: config-volume
      mountPath: /etc/nginx/conf.d # マウント先のディレクトリ
  volumes:
    - name: config-volume
      configMap:
        name: my-app-config # 利用するConfigMap名
        items:
          - key: nginx.conf
            path: default.conf # マウントされるファイル名
```
この例では、`my-app-config`の`nginx.conf`キーの値が、コンテナ内の`/etc/nginx/conf.d/default.conf`というファイルとしてマウントされます。

## 4. ConfigMapの更新と反映

### ConfigMapの更新

`kubectl edit`コマンドや、マニフェストファイルを修正して`kubectl apply`を実行することで更新できます。

```bash
# コマンドで編集
kubectl edit configmap my-app-config

# YAMLファイルを修正して再適用
# (my-app-config.yamlの内容を修正してから実行)
kubectl apply -f my-app-config.yaml
```

### 更新内容のPodへの反映

**重要な注意点として、ConfigMapを更新しても、それを参照しているPodは自動的には更新されません。**

*   **環境変数として利用している場合:** Podを再作成（削除して再作成）する必要があります。Deploymentで管理している場合は、Deploymentを再起動（ローリングアップデート）させることで反映されます。
    ```bash
    # DeploymentのPodを再起動
    kubectl rollout restart deployment <deployment-name>
    ```
*   **ボリュームとしてマウントしている場合:** マウントされたファイルは**自動的に更新**されます。ただし、コンテナ内のアプリケーションがファイルの変更を検知してリロードする機能を持っていない限り、アプリケーションの動作には反映されません。
