# コンテナのヘルスチェック: Liveness, Readiness, Startup Probes

Kubernetesでは、Pod内のコンテナが正常に機能しているかを定期的に確認するためのヘルスチェック機能として、**Liveness Probe**、**Readiness Probe**、**Startup Probe** が提供されています。これらのProbeを適切に設定することで、アプリケーションの可用性と信頼性を高めることができます。

---

## 1. Liveness Probe

Liveness Probeは、コンテナが**生きているか（running状態か）**を監視します。このProbeが失敗すると、kubeletはコンテナを強制的に再起動します。

### なぜ必要か？
アプリケーションが実行中であっても、内部でデッドロックが発生したり、メモリを過剰に消費して応答不能になったりすることがあります。このような場合、アプリケーションは外部からのリクエストに応答できませんが、プロセス自体は生きているため、Kubernetesは異常を検知できません。Liveness Probeは、このような「ゾンビ状態」のコンテナを検出し、再起動することでアプリケーションを正常な状態に復旧させます。

### 設定例
以下は、HTTP GETリクエストを使用したLiveness Probeの例です。

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: liveness-pod
spec:
  containers:
  - name: my-app
    image: nginx
    livenessProbe:
      httpGet:
        path: / # このパスに対してHTTP GETリクエストを送信
        port: 80
      initialDelaySeconds: 5  # コンテナ起動後、5秒待ってから最初のProbeを開始
      periodSeconds: 10       # 10秒間隔でProbeを実行
      timeoutSeconds: 1       # Probeのタイムアウト時間（1秒）
      failureThreshold: 3     # 3回連続で失敗した場合にコンテナを再起動
```

---

## 2. Readiness Probe

Readiness Probeは、コンテナが**リクエストを受け付ける準備ができているか**を監視します。このProbeが失敗すると、PodはServiceのエンドポイントから一時的に切り離され、新しいトラフィックがそのPodに送られなくなります。

### なぜ必要か？
コンテナが起動しても、アプリケーションがリクエストを処理できるようになるまでには時間がかかる場合があります（例：重い設定ファイルの読み込み、データベース接続の確立など）。Readiness Probeがないと、まだ準備ができていないPodにトラフィックが送られてしまい、リクエストが失敗する可能性があります。Readiness Probeを設定することで、アプリケーションが完全に準備できたことを確認してからトラフィックを流すことができます。

### 設定例
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: readiness-pod
spec:
  containers:
  - name: my-app
    image: my-custom-app
    readinessProbe:
      exec:
        command:
        - cat
        - /tmp/ready # このコマンドが成功（終了コード0）すれば準備完了とみなす
      initialDelaySeconds: 10
      periodSeconds: 5
```

---

## 3. Startup Probe

Startup Probeは、コンテナ内の**アプリケーションが正常に起動したか**を確認します。このProbeが成功するまで、Liveness ProbeとReadiness Probeは実行されません。

### なぜ必要か？
起動に非常に時間がかかるアプリケーションでは、Liveness ProbeやReadiness Probeがタイムアウトし、起動が完了する前に「異常」と判断されてコンテナが再起動されてしまう問題がありました。従来は`initialDelaySeconds`を十分に長い値に設定して対応していましたが、起動時間の見積もりは困難です。

Startup Probeは、このようなアプリケーションに対して**起動が完了するまで**の時間を確保するために使用されます。Startup Probeが成功するまで他のProbeは待機するため、安全にヘルスチェックを開始できます。

`failureThreshold` と `periodSeconds` の設定が重要で、`failureThreshold * periodSeconds` の時間内にProbeが成功しない場合、コンテナは再起動されます。

### 設定例
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: startup-pod
spec:
  containers:
  - name: my-slow-app
    image: my-slow-startup-app
    startupProbe:
      httpGet:
        path: /healthz
        port: 8080
      failureThreshold: 30 # 30回まで失敗を許容
      periodSeconds: 10    # 10秒間隔でチェック
      # この場合、最大 30 * 10 = 300秒 の起動時間が確保される
```

---

## Probeの種類

各Probeでは、以下の3つのいずれかの方法でヘルスチェックを実行できます。

| 種類 | 説明 |
|:---|:---|
| **`httpGet`** | 指定されたIPアドレス、ポート、パスに対してHTTP GETリクエストを送信します。ステータスコードが200以上400未満の場合に成功とみなされます。 |
| **`tcpSocket`** | 指定されたポートに対してTCP接続を試みます。ポートが開いていれば成功とみなされます。 |
| **`exec`** | コンテナ内で指定されたコマンドを実行します。コマンドの終了コードが0の場合に成功とみなされます。 |

## ベストプラクティス

- **Liveness ProbeとReadiness Probeの使い分け**:
  - **Liveness**: コンテナの再起動で回復する致命的な問題（デッドロックなど）を検出するために使用します。
  - **Readiness**: 一時的にリクエストを処理できない状態（初期化中、高負荷など）を検出し、トラフィックを制御するために使用します。
- **Probeのエンドポイント**: アプリケーションのヘルスチェック専用のエンドポイント（例：`/healthz`）を用意し、単にHTTP 200を返すだけでなく、データベース接続など、アプリケーションの依存関係もチェックすることが推奨されます。
- **Startup Probeの活用**: 起動に時間がかかるアプリケーションには、`initialDelaySeconds`を長く設定する代わりにStartup Probeを使用します。Startup Probeは**起動時**に一度だけアプリケーションの起動完了を確認し、成功した後はReadiness/Liveness Probeが**継続的**な監視を引き継ぎます。この役割分担により、信頼性の高いヘルスチェックが実現できます。
