# 03.3 Secretによる機密情報の管理

## 1. Secretとは

Kubernetesでアプリケーションを運用する際には、データベースのパスワード、APIキー、TLS証明書など、機密性の高い情報を扱う必要があります。これらの情報をPodの定義ファイル（マニフェスト）に直接書き込むことは、セキュリティ上非常に危険です。

`Secret`は、こうした機密情報を安全に管理・利用するためのリソースです。ConfigMapと似ていますが、機密情報を扱うことに特化しており、データがBase64でエンコードされて保存されるなどの違いがあります。（ただし、Base64は暗号化ではないため、RBACによるアクセス制御などと組み合わせることが重要です。）

### ConfigMapとの違い

| | Secret | ConfigMap |
| :--- | :--- | :--- |
| **用途** | パスワード、APIキーなどの機密情報 | 設定情報、環境変数など |
| **データ形式** | Base64エンコード | 平文 |
| **主な利用方法** | 環境変数、ボリュームマウント | 環境変数、ボリュームマウント |

## 2. マニフェストファイルからのSecret作成

Secretはマニフェストファイルを使って作成するのが一般的です。

### `stringData`フィールドを利用する方法（推奨）

`stringData`フィールドを使うと、値を平文で記述できるため、可読性が高く便利です。Kubernetesが自動的にBase64にエンコードしてくれます。

**例：データベースのユーザー名とパスワードを定義するSecret**
`db-secret.yaml`
```yaml
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
type: Opaque # 任意のデータ。デフォルト値。
stringData:
  username: "admin"
  password: "SuperSecretPassword123"
```

### `data`フィールドを利用する方法

`data`フィールドを使う場合は、値をあらかじめBase64でエンコードしておく必要があります。

**`stringData`の例と同じ内容を`data`で記述する場合：**
`db-secret-data.yaml`
```yaml
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials-data
type: Opaque
data:
  # username: "admin" -> YWRtaW4=
  username: "YWRtaW4="
  # password: "SuperSecretPassword123" -> U3VwZXJTZWNyZXRQYXNzd29yZDEyMw==
  password: "U3VwZXJTZWNyZXRQYXNzd29yZDEyMw=="
```
> **Tips:** `echo -n 'admin' | base64` のようなコマンドでBase64エンコードできます。

## 3. PodからSecretを利用する方法

作成したSecretは、Pod内のコンテナから環境変数またはボリュームとして利用できます。

### 環境変数として利用する

アプリケーションが環境変数から設定を読み込む場合に便利です。

**例：Secretの値を環境変数としてPodに渡す**
`pod-with-secret-env.yaml`
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod
spec:
  containers:
  - name: myapp-container
    image: busybox
    command: ["/bin/sh", "-c", "env"] # 環境変数を一覧表示するコマンド
    env:
      # db-credentials Secretのusernameキーの値を取得
      - name: DB_USERNAME
        valueFrom:
          secretKeyRef:
            name: db-credentials
            key: username
      # db-credentials Secretのpasswordキーの値を取得
      - name: DB_PASSWORD
        valueFrom:
          secretKeyRef:
            name: db-credentials
            key: password
```

### ボリュームとしてマウントする（設定ファイルとして利用）

設定ファイルのように、ファイルとしてコンテナ内に配置したい場合に利用します。

**例：Secretの値をファイルとしてコンテナにマウントする**
`pod-with-secret-volume.yaml`
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod-volume
spec:
  containers:
  - name: myapp-container
    image: busybox
    command: ["/bin/sh", "-c", "ls -l /etc/secret-volume && cat /etc/secret-volume/username && cat /etc/secret-volume/password"]
    volumeMounts:
    - name: secret-volume
      mountPath: "/etc/secret-volume" # このパスにマウント
      readOnly: true
  volumes:
  - name: secret-volume
    secret:
      secretName: db-credentials
```

このPodを実行すると、`/etc/secret-volume`ディレクトリに`username`と`password`というファイルが作成され、それぞれのファイルにSecretの値が書き込まれます。
