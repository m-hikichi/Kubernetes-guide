# Kubernetesにおけるリソース管理 (Requests, Limits, QoS)

Kubernetesでアプリケーションを安定稼働させるためには、コンテナが使用するコンピュータリソース（CPUやメモリ）を適切に管理することが非常に重要です。このドキュメントでは、リソースの要求量（Requests）と上限（Limits）の設定方法、そしてそれによって決まるPodの優先度（QoS Class）について解説します。

## 1. はじめに：なぜリソース管理が必要なのか？

Kubernetesクラスタでは、複数のコンテナが1つのノード（物理マシンや仮想マシン）上で同時に動作します。もし、あるコンテナが想定以上にリソースを消費してしまうと、同じノード上で動作している他のコンテナのパフォーマンスに悪影響を及ぼしたり、最悪の場合はノード全体が不安定になったりする可能性があります。

このような事態を防ぐために、KubernetesではPod内の各コンテナに対して、使用するCPUとメモリの量を指定する仕組みが提供されています。

- **Resource Requests（リソース要求）**: コンテナが最低限必要とするリソース量。Kubernetesはこの要求量を満たせるノードにPodをスケジュールします。
- **Resource Limits（リソース制限）**: コンテナが使用できるリソースの最大量。これを超えるリソース消費は許可されません。

これらの設定を適切に行うことで、リソースの効率的な利用と、アプリケーションの安定稼働を両立させることができます。

## 2. Resource Requests（リソース要求）

`requests`は、コンテナが正常に動作するために**最低限保証してほしいリソース量**を指定します。Kubernetesのスケジューラは、Podの全コンテナの`requests`の合計値を満たすだけの空きリソースがあるノードを探して、Podを配置します。

### CPU・メモリの単位

- **CPU**:
    - `1` = 1 vCPU/Core
    - `0.5` = 0.5 vCPU/Core
    - `500m` = 500 millicpu = 0.5 vCPU/Core (`m`は「ミリ」を表す)
- **メモリ**:
    - `128Mi` = 128 Mebibytes
    - `1Gi` = 1 Gibibytes
    - `M`, `G` といった単位も使えますが、2のべき乗である `Mi`, `Gi` を使うことが一般的です。

### マニフェストの書き方

以下は、nginxコンテナに対してCPUとメモリの`requests`を設定する例です。

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: resource-requests-pod
spec:
  containers:
  - name: nginx-container
    image: nginx
    resources:
      requests:
        memory: "64Mi"
        cpu: "250m" # 0.25 CPU
```

この設定により、このPodは「最低でも64MiBのメモリと0.25CPUが利用できるノード」にスケジュールされることが保証されます。

## 3. Resource Limits（リソース制限）

`limits`は、コンテナが使用できる**リソースの最大量**を定義します。これにより、特定のコンテナがリソースを使いすぎることを防ぎます。

- **CPU `limits`を超えた場合**: コンテナのCPU使用時間が制限（スロットリング）されます。アプリケーションのパフォーマンスは低下しますが、コンテナが停止することはありません。
- **メモリ `limits`を超えた場合**: コンテナはOOM (Out of Memory) Killerによって強制的に終了させられます。これは`OOMKilled`というステータスで確認できます。

### マニフェストの書き方

`requests`と`limits`を両方設定する例です。

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: resource-limits-pod
spec:
  containers:
  - name: nginx-container
    image: nginx
    resources:
      requests:
        memory: "64Mi"
        cpu: "250m"
      limits:
        memory: "128Mi"
        cpu: "500m" # 0.5 CPU
```

この設定により、コンテナは最大でも128MiBのメモリと0.5CPUまでしか使用できません。

## 4. QoS (Quality of Service) Classes

Kubernetesは、`requests`と`limits`の設定に基づいて、Podを以下の3つの**QoSクラス**のいずれかに自動的に分類します。このクラスは、ノードのリソースが枯渇した際に、どのPodを優先的に停止させるかを決定するために使用されます。

| QoSクラス | 条件 | 特徴 |
| :--- | :--- | :--- |
| **Guaranteed** | Pod内の**全てのコンテナ**で、CPUとメモリの**`requests`と`limits`が設定されており、かつその値が等しい**。 | 最も優先度が高い。ノードのリソースが不足した場合でも、最後まで停止させられにくい。重要な本番環境のアプリケーションに適している。 |
| **Burstable** | Pod内の少なくとも1つのコンテナで、CPUまたはメモリの`requests`が設定されているが、`Guaranteed`の条件を満たさない。 | 中間の優先度。リソース使用量が変動する可能性があるアプリケーションに適している。`requests`で指定した分は保証されるが、`limits`までは状況に応じてリソースを使用できる。 |
| **BestEffort** | Pod内のどのコンテナにも、CPUとメモリの`requests`と`limits`が設定されていない。 | 最も優先度が低い。ノードのリソースが不足した場合に、最初に停止させられる対象となる。開発環境や重要度の低いバッチ処理などに適している。 |

## 5. 設定内容と使用状況の確認方法

### 設定内容の確認

`kubectl describe pod`コマンドで、Podに適用されている`requests`, `limits`, `QoS Class`を確認できます。

```bash
# Podを作成
kubectl apply -f resource-limits-pod.yaml

# Podの詳細情報を表示
kubectl describe pod resource-limits-pod
```

出力結果の中に、以下のようなセクションがあります。

```
    Limits:
      cpu:     500m
      memory:  128Mi
    Requests:
      cpu:        250m
      memory:     64Mi
...
QoS Class:       Burstable
```

### リソース使用状況の確認

`kubectl top pod`コマンドで、Podが現在どれくらいのリソースを使用しているかを確認できます。（Metrics Serverがクラスタにインストールされている必要があります）

```bash
kubectl top pod resource-limits-pod
```

出力例：
```
NAME                  CPU(cores)   MEMORY(bytes)
resource-limits-pod   1m           5Mi
```

## 6. ベストプラクティス

- **可能な限り全てのコンテナに`requests`と`limits`を設定する**: 特に本番環境では、`BestEffort`クラスのPodは避けるべきです。
- **`requests`と`limits`を同じ値にする (`Guaranteed`)**: データベースなど、パフォーマンスが重要なステートフルなアプリケーションには、`Guaranteed`クラスを使用することが推奨されます。
- **アプリケーションの特性を理解する**: アプリケーションが通常時とピーク時でどれくらいのリソースを消費するのかを計測し、適切な`requests`と`limits`の値を設定しましょう。最初は少し大きめに設定し、モニタリングしながら徐々に最適化していくのが良いアプローチです。
